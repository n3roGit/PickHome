# Local development (npm run dev)
DATABASE_URL="file:../data/pickhome.db"
PICKHOME_DATA_DIR=./data

# Optional — reserved for future session hardening
SESSION_SECRET=change-me-in-production

# Docker Compose (set in shell or .env next to docker-compose.yml)
# PICKHOME_PORT=3000

# Optional — public base URL for iCal feed links (defaults to http://localhost:3000)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
